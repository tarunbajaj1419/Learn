'use strict';
var $       = require('./$')
  , SPECIES = require('./$.wks.js')('species');
module.exports = function(C){
  if(require('./$.support-desc.js') && !(SPECIES in C))$.setDesc(C, SPECIES, {
    configurable: true,
    get: function(){ return this; }
  });
};