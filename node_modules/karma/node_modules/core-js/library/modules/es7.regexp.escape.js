// https://github.com/benjamingr/RexExp.escape
var $def = require('./$.def.js')
  , $re  = require('./$.replacer.js')(/[\\^$*+?.()|[\]{}]/g, '\\$&');
$def($def.S, 'RegExp', {escape: function escape(it){ return $re(it); }});
