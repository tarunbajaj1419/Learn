// 20.1.2.5 Number.isSafeInteger(number)
var $def      = require('./$.def.js')
  , isInteger = require('./$.is-integer.js')
  , abs       = Math.abs;

$def($def.S, 'Number', {
  isSafeInteger: function isSafeInteger(number){
    return isInteger(number) && abs(number) <= 0x1fffffffffffff;
  }
});