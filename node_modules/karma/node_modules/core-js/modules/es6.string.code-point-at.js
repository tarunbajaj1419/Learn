'use strict';
var $def = require('./$.def.js')
  , $at  = require('./$.string-at.js')(false);
$def($def.P, 'String', {
  // 21.1.3.3 String.prototype.codePointAt(pos)
  codePointAt: function codePointAt(pos){
    return $at(this, pos);
  }
});