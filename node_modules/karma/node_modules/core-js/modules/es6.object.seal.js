// 19.1.2.17 Object.seal(O)
var isObject = require('./$.is-object.js');

require('./$.object-sap.js')('seal', function($seal){
  return function seal(it){
    return $seal && isObject(it) ? $seal(it) : it;
  };
});