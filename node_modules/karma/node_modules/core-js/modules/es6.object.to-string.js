'use strict';
// 19.1.3.6 Object.prototype.toString()
var classof = require('./$.classof.js')
  , test    = {};
test[require('./$.wks.js')('toStringTag')] = 'z';
if(test + '' != '[object z]'){
  require('./$.redef.js')(Object.prototype, 'toString', function toString(){
    return '[object ' + classof(this) + ']';
  }, true);
}