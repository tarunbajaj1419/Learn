var $def   = require('./$.def.js')
  , create = require('./$').create
  , define = require('./$.object-define.js');

$def($def.S + $def.F, 'Object', {
  make: function(proto, mixin){
    return define(create(proto), mixin);
  }
});