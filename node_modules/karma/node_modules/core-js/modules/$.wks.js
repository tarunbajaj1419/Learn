var store  = require('./$.shared.js')('wks')
  , Symbol = require('./$.global.js').Symbol;
module.exports = function(name){
  return store[name] || (store[name] =
    Symbol && Symbol[name] || (Symbol || require('./$.uid.js'))('Symbol.' + name));
};