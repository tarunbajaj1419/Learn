// https://github.com/mathiasbynens/String.prototype.at
'use strict';
var $def = require('./$.def.js')
  , $at  = require('./$.string-at.js')(true);
$def($def.P, 'String', {
  at: function at(pos){
    return $at(this, pos);
  }
});