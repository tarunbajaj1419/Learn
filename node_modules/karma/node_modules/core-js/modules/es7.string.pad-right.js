'use strict';
var $def = require('./$.def.js')
  , $pad = require('./$.string-pad.js');
$def($def.P, 'String', {
  padRight: function padRight(maxLength /*, fillString = ' ' */){
    return $pad(this, maxLength, arguments[1], false);
  }
});